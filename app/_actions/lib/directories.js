import path from 'path';

export const ROOT_DIR = path.join(process.cwd(), 'app/_features/');

export const ATTRIBUTES_DIR = `${ROOT_DIR}/${'attributes'}`;

export const SEGMENTS_DIR = `${ROOT_DIR}/${'segments'}`;
export const FEATURES_DIR = `${ROOT_DIR}/${'features'}`;

export const GROUPS_DIR = `${ROOT_DIR}/${'groups'}`;
